// Change nom de fichier a diday.config.js, Il est dans la .gitignore
// Ajouté les variable ici, mainApiOrigin c'est l'origin de server express
// Je ne voulais pas metre mon IP sur Git HUB.

export default (() => {
  return {
    mainApiOrigin: '',
  };
})();
